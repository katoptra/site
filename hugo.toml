# Site settings
baseURL = "https://katoptra.org/"
locale = "en-US"
title = "Verified Open-Source Software Mirrors - katoptra.org"
theme = "ijosh"
enableRobotsTXT = true
disableKinds = ["section", "taxonomy", "RSS"]

# Copyright
copyright = "&copy;2026 Josh Vaughen"

[sitemap]
  changeFreq = 'monthly'
  disable = false
  filename = 'sitemap.xml'
  priority = 0.8

[params]

  # Metadata for search engines and social sharing
  author = "Josh Vaughen"
  description = "Fast, verified mirrors of popular open-source software, synced on a schedule by open pipelines and served on Cloudflare's global network."

  # Cloudflare Web Analytics beacon token (public; omit to disable the beacon)
  # cloudflareBeaconToken = ""
